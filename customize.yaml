sensor.garage_open_close:
  templates:
    icon_color: >
      if (state === 'Open') return 'var(--primary-text-color)';
      else return 'var(--secondary-text-color)';

sensor.front_door_open_close:
  templates:
    icon_color: >
      if (state === 'Open') return 'var(--secondary-text-color)';
      else return 'var(--primary-text-color)';

group.kitchen_cabinet_lights:
  templates:
    icon_color: >
      if (state === 'on') return 'var(--secondary-text-color)';
      else return 'var(--primary-text-color)';

switch.all_lights_off:
  icon_color: '#FC2B36'

binary_sensor.desktop_pc:
  templates:
    icon_color: >
      if (state === 'on') return 'var(--secondary-text-color)';
      else return 'var(--primary-text-color)';
    icon: mdi:desktop-tower
    
binary_sensor.work_laptop:
  templates:
    icon_color: >
      if (state === 'on') return 'var(--secondary-text-color)';
      else return 'var(--primary-text-color)';
    icon: mdi:laptop

binary_sensor.diskstation:
  templates:
    icon_color: >
      if (state === 'on') return 'var(--secondary-text-color)';
      else return 'var(--primary-text-color)';
    icon: mdi:server

binary_sensor.mini_pc:
  templates:
    icon_color: >
      if (state === 'on') return 'var(--secondary-text-color)';
      else return 'var(--primary-text-color)';
    icon: mdi:desktop-tower

sensor.man_cave_door_open_close:
  templates:
    icon_color: >
      if (state === 'Open') return 'var(--secondary-text-color)';
      else return 'var(--primary-text-color)';

device_tracker.akmolina28_andrewsphone:
  entity_picture: /local/images/people/andrew2.png